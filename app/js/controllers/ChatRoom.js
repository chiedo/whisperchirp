'use strict';

whisperChirpApp.controller('ChatRoomController',
  function ChatRoomController($scope,  $location, ChatRoom) {
    var filter = $scope.filter = {
      cuisine: [],
      price: null,
      rating: null
    };
});
